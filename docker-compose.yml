version: '3.1'

services:

  mongo:
    image: mongo:5.0.8
    container_name: mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin

  server:
    build:
      context: ./server
    ports:
      - 5000:5000
    restart: always
    depends_on:
      - mongo
    volumes:
      - ./server:/app
